# Shared local development hints.
# Copy app-specific files from apps/server/.env.example when needed.

WEB_PORT=5173
SERVER_PORT=4000
API_KEY_ENCRYPTION_SECRET="replace-with-a-long-local-random-secret"
